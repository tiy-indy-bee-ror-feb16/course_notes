<nav class="navbar navbar-inverse navbar-fixed-top">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link_to "Cheep", root_path, class: 'navbar-brand' %>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
          <% if current_user %>
            <ul class="nav navbar-nav navbar-right">
              <li><%= link_to "Profile", user_path(id: 'self') %></li>
              <li><%= link_to "Settings", edit_user_path(current_user) %></li>
              <li><%= link_to "Sign out", logout_path, method: :delete, remote: true %></li>
            </ul>
          <% else %>
            <%= simple_form_for :session, url: login_create_path, html: {class: 'navbar-form navbar-right'}, remote: true do |f| %>
              <%= f.input :identifier, placeholder: 'Email or username', label: false %>
              <%= f.input :password, placeholder: 'Password', label: false %>
              <%= f.button :submit, 'Sign in', class: 'btn btn-success' %>
            <% end %>
          <% end %>
        </div><!--/.navbar-collapse -->
      </div>
    </nav>
